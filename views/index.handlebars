<div class="container">

    <p>Stuff will go here, please?</p>
    {{#each articles}}
        <p data-id="{{this._id}}">{{this.title}} <br /> <a href="{{this.link}}">{{this.link}}</a></p>
    {{/each}}


</div>

<div id="wrapper">
    <div id="notes"></div>
</div>